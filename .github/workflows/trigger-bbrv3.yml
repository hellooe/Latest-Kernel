name: Build BBRv3 Kernel

on:
  workflow_dispatch:
    inputs:
      kernel_version:
        description: 'Kernel version (e.g., 6.12, 7.0) – will use linux-${version}.y branch'
        required: true
        default: '6.12'
        type: string
      bbr_patch_url:
        description: 'URL to the BBRv3 patch file (MUST be provided)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  call-build:
    uses: ./.github/workflows/build-kernel.yml
    with:
      kernel_version: ${{ github.event.inputs.kernel_version }}
      apply_bbr3_patch: true
      config_file: 'config'
      release_tag_prefix: 'bbrv3-'
      release_name_prefix: 'BBRv3 Kernel'
      bbr_patch_url: ${{ github.event.inputs.bbr_patch_url }}
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
